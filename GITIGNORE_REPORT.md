# Gitignore Report

Date: 2026-06-30

## Detected Project Type

Detected type: Unity.

Evidence:

- `ProjectSettings/ProjectVersion.txt` exists.
- `Packages/manifest.json` exists.
- Unity editor version is `6000.3.10f1`.
- Standard Unity folders are present: `Assets/`, `Packages/`, `ProjectSettings/`, `Library/`, `Builds/`, `Logs/`, `UserSettings/`.

## What Was Updated

The existing `.gitignore` already contained the core Unity exclusions from the prior size cleanup pass. I preserved those rules and added a few safe project-wide additions:

- `Obj*/` for Unity/IDE object output variants.
- `Assets/AssetStoreTools*` for optional local Asset Store tooling.
- `StreamingAssets/aa.meta` for Addressables-generated metadata if Addressables is added later.
- `*.tmp`, `*.bak`, `*.swp` for temporary editor files.
- `.DS_Store`, `Thumbs.db`, `Desktop.ini` for OS junk files.

No gameplay code, project structure, or assets were changed.

## Major Ignore Rules And Why

| Rule | Why it is ignored |
|---|---|
| `[Ll]ibrary/` | Unity import cache and generated artifact database. It is regenerated locally and should not be committed. |
| `[Tt]emp/` | Unity temporary files. |
| `[Oo]bj/`, `[Oo]bj*/` | Generated object/build intermediates from Unity, IDEs, or compilers. |
| `[Bb]uild/`, `[Bb]uilds/` | Exported builds, including Windows player output. These are reproducible artifacts, not source. |
| `[Ll]ogs/` | Local build/test/editor logs. |
| `[Uu]ser[Ss]ettings/` | User-local Unity editor settings. |
| `[Mm]emoryCaptures/`, `[Rr]ecordings/` | Unity profiling/recording output. |
| `.vs/`, `.idea/`, `.vscode/` | IDE-local state. |
| `*.csproj`, `*.sln`, `*.user`, `*.userprefs` | Unity/IDE-generated project files and local user config. |
| `*.pdb`, `*.mdb` | Debug symbols generated by Unity/Mono builds. |
| `*.dmp`, `*.stackdump` | Crash dumps. |
| `/*.log`, `/batch_*.log`, `/standalone_*.log` | Root-level generated build/smoke logs. |
| `unity_render_check*.png`, `generated_*screenshot*.png`, `visual_check*.png` | Root-level generated visual/debug screenshots. |
| `*.apk`, `*.aab`, `*.app`, `*.xcarchive`, `*.unitypackage` | Exported packages/build artifacts. |
| `.DS_Store`, `Thumbs.db`, `Desktop.ini` | OS metadata/junk files. |

## Folders That Should Not Be Committed

These folders are generated, local, or exported output and should remain untracked:

- `Library/`
- `Temp/`
- `Obj/`
- `Build/`
- `Builds/`
- `Logs/`
- `UserSettings/`
- `MemoryCaptures/`
- `Recordings/`
- `.vs/`
- `.idea/`
- `.vscode/`

## Files That Must Remain Trackable

These are important source/project files and are not ignored by the project `.gitignore`:

- `Assets/**/*.cs`
- `Assets/**/*.unity`
- `Assets/**/*.prefab`
- `Assets/**/*.asset`
- `Assets/**/*.shader`
- `Assets/**/*.png` and matching `.meta` files
- `Assets/**/*.meta`
- `Packages/manifest.json`
- `Packages/packages-lock.json`
- `ProjectSettings/**`
- `README.md`
- `SIZE_AUDIT.md`
- `SIZE_OPTIMIZATION_PLAN.md`
- `SIZE_BUDGET.md`
- `scripts/size_report.ps1`
- `scripts/size_report.sh`
- `.gitattributes`
- `.gitignore`

Note: `.gitattributes` currently marks generated PNG art and common large binary art/audio formats for Git LFS. That does not ignore those files; it only changes how Git stores them when Git LFS is enabled.

## Tracking Check

Checked via the parent Git worktree because this directory is under `C:/Users/lyush`.

Result:

- `Library/`, `Builds/`, `Logs/`, and `UserSettings/` appear as ignored.
- `git ls-files` returned no tracked files under `Library/`, `Builds/`, `Logs/`, `UserSettings/`, `Temp/`, or `Obj/`.
- The project directory itself currently appears untracked from the parent Git worktree.

Because no generated/cache/build files were found as tracked, no cleanup command was run.

